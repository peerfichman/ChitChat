const RowCell = ({ value }) => {
    return (
        <td className="w-10 px-6 py-3 text-start text-xs font-medium text-gray-500">
            {value}
        </td>
    );
};

export default RowCell;
